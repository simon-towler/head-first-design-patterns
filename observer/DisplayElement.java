public interface DisplayElement {
  /*
   * The DisplayElement interface includes just one method, display(), that we
   * will call when the display element needs to be displayed
   */
  public void display();
}
